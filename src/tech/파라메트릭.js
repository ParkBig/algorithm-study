const 파라메트릭 = (arr) => {
  star = 0; // or 최소값;
  end = Math.max(...arr); // 최대값

  let result = 0;
  while (start <= end) {
    let mid = parseInt((start + end) / 2);
    let total = 0;
    // for () {}
    if (total < n) {
      // end나 start 값 -1 or +1로 조정
      // result 값 지정가능
    } else {
      // end나 start 값 -1 or +1로 조정
      // result 값 지정가능
    }
  }
};
 